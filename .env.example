# Database Configuration
# Copy this to .env and update with your actual credentials

# MySQL Connection Settings
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=Kuntal@2006
DB_NAME=campus_care

# Flask Configuration
FLASK_ENV=development
FLASK_DEBUG=True
SECRET_KEY=campus_care_secret_key_change_in_production

# Optional: Enable for production (use python -c "import secrets; print(secrets.token_hex(32))")
# SECRET_KEY=your_generated_secret_key_here
